services:
  - type: web
    name: demo-retail-clone
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
